<h3>
	<center>
		<%= link_to '~ Add new Category ~',new_category_path %>
	</center>
</h3>

<%count = 1%>

 <% @categories.each do |category| %>
    
    <h2>
    	<%= "#{count}." %> <%=link_to category.name,category %>
    </h2>

    <% count += 1 %>

 <% end %>